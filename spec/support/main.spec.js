describe("When window onload", function () {

    it("should have buttons", function () {
       console.log(this);
    });
});

describe("Check click time", function () {
    it("should have click at once", function () {
       // const document = new JSDOM(`<!DOCTYPE html><body></body>`);
        const temp = [1,1,1,1,1,1,1,0,1];
        expect(temp[temp.length -1]).not.toBe(0);
    });
});



describe("moveToLeft", function () {
    it("should move button to the left", function () {

    });
});



